## 🧩 گام چهارم – ارزیابی (سپهر نوروزی چاکلی)

### **تحلیل جامع رعایت اصول و تأثیر آن بر گسترش‌پذیری سیستم**

در گام‌های دوم و سوم مشاهده شد که بسیاری از مشکلات اساسی در طراحی نسخهٔ اولیه پروژه از آنجا ناشی می‌شد که اصول بنیادین شی‌گرایی همچون **SRP، OCP، LSP، ISP، DIP، PLK و CRP** در ساختار کد رعایت نشده بودند. این وضعیت موجب شده بود تا بخش‌های مختلفِ سیستم به‌شدت به‌هم وابسته باشند و هرگونه تغییر یا افزودن ویژگی جدید عملاً نیازمند ویرایش چندین فایل و کلاس شود.  

به‌طور مشخص، کلاس‌های کلیدی همچون `ReservationService`، `Reservation`، `Customer` و `Room` نقش‌های متعددی را هم‌زمان بر عهده داشتند؛ بخشی از آن‌ها مسئول مدیریت منطق رزرو، بخشی دیگر منطق ارتباطی و حتی بخشی مسئول پردازش پرداخت و ارسال اعلان بودند. این در حالی است که منطق صحیح شی‌گرایی اقتضا می‌کند هر کلاس دقیقاً **یک مسئولیت واحد** داشته باشد تا تغییر در یک جنبه از سیستم بر سایر بخش‌ها اثر منفی نگذارد.

در نتیجه، زمانی که در گام اول دو قابلیت جدید — یعنی **افزودن روش جدید پرداخت حضوری (`onSitePayment`)** و **افزودن روش ارسال پیامک (`sendSmsMessage`)** — افزوده شدند، توسعه‌دهندگان ناچار شدند چندین کلاس را تغییر دهند تا این قابلیت‌ها در سیستم اعمال شوند. این مسئله نه‌تنها منجر به تکرار کد، بلکه باعث افزایش ریسک بروز خطا و دشواری در آزمایش نرم‌افزار نیز شد.

اما با اعمال بازطراحی در گام سوم، وابستگی‌های مستقیم به پیاده‌سازی‌ها (Concrete Classes) حذف و جای آن‌ها ساختار انتزاعی (Interfaces یا Abstractions) اضافه شد. به این ترتیب، هر کلاس اکنون تنها از طریق سطوح انتزاع با سایر اجزا ارتباط دارد؛ یعنی دقیقاً همان مفهومی که **اصل وارونگی وابستگی (DIP)** بر آن تأکید می‌کند.  

به‌علاوه، با پیاده‌سازی اصولی مانند **قانون دمیلتر یا “اصل کمترین آگاهی” (PLK)**، دیگر هیچ کلاسی وارد جزئیات داخلی کلاس‌های دیگر نمی‌شود، بلکه برای انجام وظایف خود از متدهای واسط بهره می‌گیرد؛ به این ترتیب هم انسجام درونی هر کلاس افزایش یافته و هم میزان وابستگی میان اجزا کاهش یافته است.  

نتیجهٔ مستقیم رعایت این اصول آن است که اکنون اگر تیم توسعه بخواهد قابلیت جدیدی به پروژه بیفزاید، دیگر نیازی به تغییر در کلاس‌های قبلی نیست. کافی است فقط یک کلاس جدید بسازد که قرارداد (Interface) مربوطه را پیاده‌سازی کند. این همان چیزی است که **اصل باز-بسته (Open/Closed Principle)** وعده می‌دهد: کد باید برای *گسترش* باز باشد، اما برای *تغییر* بسته بماند.

---

